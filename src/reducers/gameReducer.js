// import {FETCH_TOP_SCORES} from '../actions/fetchTopScoresAction';

export default function game(
  state = {
    game: '',
  }, action) {
  switch (action.type) {
    // case FETCH_TOP_SCORES:
    //   return state;

    default:
      return state;
  }
}